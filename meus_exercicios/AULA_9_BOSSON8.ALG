Algoritmo "semnome"

Var
// Seção de Declarações das variáveis 
   V, F, RES : logico

Inicio
// Seção de Comandos, procedimento, funções, operadores, etc...
// Função : "OU" e "E" e NOT (Tudo junto)

   V <- Verdadeiro
   F <- Falso
   RES <- (nao nao F ou nao nao F) e (V ou F)
   escreva (RES )


Fimalgoritmo